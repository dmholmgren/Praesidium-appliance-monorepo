# ═══════════════════════════════════════════════════════════════
# Praesidium Platform — Python Dependencies

# ─── Web Framework ───────────────────────────────────────────
fastapi>=0.110,<0.116
uvicorn[standard]>=0.29,<0.35
python-multipart>=0.0.9,<1.0
# ─── Templates ───────────────────────────────────────────────
jinja2>=3.1,<4.0
# ─── Database ────────────────────────────────────────────────
sqlalchemy>=2.0,<2.1
pymysql>=1.1,<2.0
cryptography>=42.0,<44.0
alembic>=1.13,<1.15
# ─── Redis ───────────────────────────────────────────────────
redis>=5.0,<6.0
# ─── Background Jobs (Rule 8) ───────────────────────────────
rq>=1.16,<2.0
rq-scheduler>=0.13,<1.0
# ─── HTTP Client ─────────────────────────────────────────────
httpx>=0.27,<1.0
# ─── AI SDKs (accessed ONLY via AIService — Rule 4) ─────────
anthropic>=0.28,<1.0
openai>=1.30,<2.0
# ─── Search ──────────────────────────────────────────────────
meilisearch>=0.31,<1.0
# ─── Auth ────────────────────────────────────────────────────
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
python-ldap>=3.4,<4.0
itsdangerous>=2.1,<3.0
# ─── Utilities ───────────────────────────────────────────────
python-dotenv>=1.0,<2.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
# ─── Testing (install via: pip install -r requirements-test.txt) ──
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0
# DMS & Auth Module — Python Dependencies
# Add these to the main requirements.txt on MAIN-PRD-WEB-01
# Core (may already be installed from Chat 0 — safe to re-specify)
alembic>=1.13.0
SQLAlchemy>=2.0
# COMP 1: CIFS Storage Adapter
httpx>=0.27.0
# COMP 2: File Crawler
redis>=5.0
rq>=1.16
# COMP 3: OCR Pipeline
pytesseract>=0.3.10
pdfplumber>=0.11.0
Pillow>=10.0
python-docx>=1.1.0
openpyxl>=3.1.0
extract-msg>=0.48.0
# COMP 4: Meilisearch
# (uses httpx — already listed above)
# COMP 5: Time Tracking
# (uses FastAPI — already in base requirements)
# COMP 7: WebDAV
WsgiDAV>=4.3.0
# COMP 8: Document Comparison
# (uses python-docx and difflib — difflib is stdlib)
# COMP 9: Legal Research
# COMP 12: Citation Suite
# (uses regex — stdlib)
# COMP 13-14: Office Add-Ins
PyJWT>=2.8.0
# COMP 15: Scanning Portal
# (uses existing OCR deps)
# COMP 16: LDAPS Auth
ldap3>=2.9.1
python-ldap>=3.4.4
# COMP 17: Azure AD Auth
# File Bridge container (separate requirements-filebridge.txt)
# fastapi>=0.110.0
# uvicorn>=0.29.0
# Chat 4 — Case Dashboard — Python Dependencies
# Install on WEB-01 and PROC-01:
#   sudo pip install -r requirements-dashboard.txt --break-system-packages
# Then append to Docker requirements:
#   sudo bash -c 'cat requirements-dashboard.txt >> requirements.txt'
rapidfuzz>=3.6.0
PyMuPDF>=1.24.0
# tesseract-ocr and poppler-utils are system packages — see install guide
pymysql>=1.1.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
elasticsearch[async]>=8.0.0,<9.0.0
pgvector>=0.2.5
aiofiles>=23.0
