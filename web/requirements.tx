# Praesidium Series 2.0 — requirements.txt
# Python 3.12
# Database: PostgreSQL 16 via asyncpg (async) and psycopg2-binary (sync/alembic)
# aiomysql and pymysql REMOVED — MariaDB is gone.
# Search: Elasticsearch 8 replacing Meilisearch.

# ── Web Framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0
itsdangerous>=2.2.0
python-jose[cryptography]>=3.3.0

# ── Database — PostgreSQL (replaces MariaDB) ─────────────────────────────────
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0          # async driver — SQLAlchemy postgresql+asyncpg://
psycopg2-binary>=2.9.9   # sync driver — Alembic autogenerate, scripts
alembic>=1.13.0

# ── Vector extension support ─────────────────────────────────────────────────
pgvector>=0.2.5          # Python bindings for pgvector PostgreSQL extension

# ── Search — Elasticsearch 8 (replaces Meilisearch) ─────────────────────────
elasticsearch[async]>=8.0.0,<9.0.0

# ── Cache / Queue ────────────────────────────────────────────────────────────
redis>=5.0.4
rq>=1.16.2
rq-scheduler>=0.13.1

# ── AI ───────────────────────────────────────────────────────────────────────
anthropic>=0.28.0

# ── Auth & Security ──────────────────────────────────────────────────────────
passlib[bcrypt]>=1.7.4
cryptography>=42.0.0
python-ldap>=3.4.4
msal>=1.28.0             # Azure AD auth adapter
google-auth>=2.29.0      # Google auth adapter

# ── Document Processing ──────────────────────────────────────────────────────
python-docx>=1.1.2
pypdf>=4.2.0
pdfplumber>=0.11.0
pytesseract>=0.3.10
Pillow>=10.3.0
python-magic>=0.4.27
chardet>=5.2.0

# ── Email / Calendar Adapters ────────────────────────────────────────────────
exchangelib>=5.2.0       # EWS adapter
O365>=2.0.35             # Microsoft Graph adapter
google-api-python-client>=2.129.0

# ── Background / Scheduling ──────────────────────────────────────────────────
celery>=5.4.0
apscheduler>=3.10.4

# ── HTTP / Networking ────────────────────────────────────────────────────────
aiohttp>=3.9.5
requests>=2.32.0
httpx>=0.27.0

# ── Templating / Frontend ────────────────────────────────────────────────────
jinja2>=3.1.4

# ── Data / Validation ────────────────────────────────────────────────────────
pydantic>=2.7.1
pydantic-settings>=2.2.1
python-dateutil>=2.9.0
numpy>=1.26.4
pandas>=2.2.2

# ── Billing / Payments ───────────────────────────────────────────────────────
stripe>=9.6.0

# ── E-signature ──────────────────────────────────────────────────────────────
docusign-esign>=3.25.0

# ── SMS / Communications ─────────────────────────────────────────────────────
twilio>=9.1.0

# ── Telemetry / Logging ──────────────────────────────────────────────────────
structlog>=24.1.0
sentry-sdk[fastapi]>=2.3.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.6
pytest-cov>=5.0.0
factory-boy>=3.3.0
faker>=25.2.0

# ── Dev Tools ────────────────────────────────────────────────────────────────
black>=24.4.2
ruff>=0.4.4
mypy>=1.10.0
pre-commit>=3.7.1
