# Copy to .env in each instance dir and fill with real values (kept out of git).
# Stalwart recovery admin password — compose: STALWART_RECOVERY_ADMIN=admin:${RECOVERY_ADMIN_PASSWORD}
RECOVERY_ADMIN_PASSWORD=
# PostgreSQL password for the 'stalwart' DB user (praesidium-mail only)
STALWART_DB_PASSWORD=
